## To run 
1. pull from backend-auth branch
2. requirement :- pip install flask flask_cors bcrypt flask_mail PyJWT
3. python app.py
4. create .env in /backend dir and there put SECRET_KEY=HELLO_WORLD
5. inside .env file add:
   MAIL_USERNAME=your_gmail
   MAIL_PASSWORD=your_app_password_from_gmail
